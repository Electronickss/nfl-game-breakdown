# Logos

Team logo PNGs used for chart annotations.

Logos are loaded from `nflfastR::teams_colors_logos` at runtime via `load_logos()` in `scripts/wp-functions.R`. The local PNGs in this directory are historical artifacts and are not used by the current codebase.
